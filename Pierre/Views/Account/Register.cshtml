@* @{Layout = "_Layout";}

@using Pierre.ViewModels
@model RegisterViewModel

<div class="card">
  <div class="card-header">
  <h3>Create Login</h3>
  </div>
  <div class="card-body">
    <p class="card-text">
      <div class="col-auto">
        @using (Html.BeginForm("Register", "Account", FormMethod.Post)){
          <p>User Name</p>
          @Html.TextBoxFor(user=> user.Email, new  {@class = "form-control", placeholder = "User Name", required="required"})
        <br>
          @Html.LabelFor(user=> user.Password)
          @Html.PasswordFor(user=> user.Password, new  {@class = "form-control", placeholder = "Password", required="required"})
        <br>
          @Html.LabelFor(user=> user.ConfirmPassword)
          @Html.PasswordFor(user=> user.ConfirmPassword, new  {@class = "form-control", placeholder = "Confirm Password", required="required"})
        <br>
          <input type="submit" value="Register" class="btn btn-info" />
        }
      </p>
    </div>
  </div>
</div> *@

@{Layout = "_Layout";}

@using Pierre.ViewModels;
@model RegisterViewModel;

<h2>Register a new user</h2>
<hr />
@using (Html.BeginForm("Register", "Account", FormMethod.Post)){
  @Html.LabelFor(user => user.Email)
  @Html.TextBoxFor(user=> user.Email, new {@class = "form-control", required="required"})
  <br>
  @Html.LabelFor(user=> user.Password)
  @Html.PasswordFor(user=> user.Password, new {@class = "form-control", required="required"})
  <br>
  @Html.LabelFor(user=> user.ConfirmPassword)
  @Html.PasswordFor(user=> user.ConfirmPassword, new {@class = "form-control", required="required"})
  <br>
  <input type="submit" value="Register" class="btn btn-dark"  />
}